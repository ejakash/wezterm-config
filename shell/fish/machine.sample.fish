# Per-machine shell values. Copy to ~/.config/fish/conf.d/00-machine.fish and edit.
# (Copied, not symlinked — install.fish never touches it.)
set -gx TERMINAL_REPO_DIR /mnt/d/labs/wezterm   # this repo's Linux path
set -gx SMART_CD_ROOT /mnt/d                    # fuzzy-cd search root
